type Album {
  
  id: ID!
  urlRym: String!
  idMbz: String
  
  title: String!
  
  rating: Int!
  review: String
  
}


type Query {
  albums: [Album]!
  album(id: ID!): Album
}


type Mutation {
  addAlbum(urlRym: String!, rating: Int): Movie!
}